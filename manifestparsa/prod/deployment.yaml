apiVersion: apps/v1
kind: Deployment
metadata:
  name: landingpage-parsa-app
  namespace: projectNLP
  labels:
    app: landingpage-parsa
spec:
  replicas: 2
  selector:
    matchLabels:
      app: landingpage-parsa
  template:
    metadata:
      labels:
        app: landingpage-parsa
    spec:
      containers:
      - name: landingpage-parsa
        image: docker.io/diwamln/landingpage-parsa:build-12-7673597
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: "200m"
            memory: "256Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: landingpage-parsa-service
  namespace: testing
spec:
  selector:
    app: landingpage-parsa
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30051 # Port akses manual via IP VPS
  type: NodePort
